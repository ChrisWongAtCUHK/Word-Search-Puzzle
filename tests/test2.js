/*
 * Start the puzzle 
 */
function startPuzzle(){
	var puzzle = {};
	puzzle.words = 
	[ 
					{
						"hiragana": ["りんご"],
						"color": "#ff0000"
					},
					{
						"hiragana": ["おとこのこ"],
						"color": "#800000"
					}
	];

	var a = puzzle.words[0].hiragana;
	var b = puzzle.words[1].hiragana;

	puzzle.grid = 
		[
			'+', '+', '+', '+', '+', '+', '+', '+', '+', '+',  
			'+', '+', '+', '+', '+', '+', '+', '+', '+', '+',  
			'+', '+', '+', '+', '+', '+', '+', '+', '+', '+',  
			'+', '+', '+', '+', '+', '+', '+', '+', '+', '+',  
			'+', 'こ', 'り', 'ん', 'ご', '+', '+', '+', '+', '+',  
			'+', 'の', '+', '+', '+', '+', '+', '+', '+', '+',  
			'+', 'こ', '+', '+', '+', '+', '+', '+', '+', '+',  
			'+', 'と', '+', '+', '+', '+', '+', '+', '+', '+',  
			'+', 'お', '+', '+', '+', '+', '+', '+', '+', '+',  
			'+', '+', '+', '+', '+', '+', '+', '+', '+', '+'  
		];

	puzzle.size = 10;
	puzzle.cellSize = 30;
	puzzle.length = 350;
	return puzzle;
}


